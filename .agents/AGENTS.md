# Messaging Agent - Workspace Rules

- **KNOWLEDGE VAULT**: Before you execute any task in this workspace, you MUST read `.agents/PROJECT_KNOWLEDGE.md` to gain full context of the Proxmox environment, Staging/Prod parity, and the dynamic multi-app deploy-agent configuration.
- **ENVIRONMENT AWARENESS**: Verify your hostname (`hostname`) and environment variables. The DevBox is `10.10.10.96`, the Proxmox Host is `65.108.8.252`.
